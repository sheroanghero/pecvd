<UserControl x:Class="MECF.Framework.Simulator.Core.VCE.SiasunVCE.SimSiasunVCEView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MECF.Framework.Simulator.Core.Commons"
             mc:Ignorable="d" 
             xmlns:commons="clr-namespace:MECF.Framework.Simulator.Core.Commons"
             Height="900"  Width="1800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"></RowDefinition>
            <RowDefinition Height="350"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <commons:SocketTitleView Grid.Row="0"></commons:SocketTitleView>


        <commons:SerialPortTitleView Grid.Row="0"></commons:SerialPortTitleView>

        <Canvas  Grid.Row="1"   Width="1000" HorizontalAlignment="Left">
            <Label Content="{Binding WaferMap}" Width="380"  Height="30"  HorizontalContentAlignment="Left"  Background="LightBlue" BorderThickness="2" Canvas.Left="202" Canvas.Top="62"></Label>

            <CheckBox Content="Present" IsChecked="{Binding IsPresent}" Canvas.Left="36" Canvas.Top="31"></CheckBox>

            <CheckBox Content="Protrude" IsChecked="{Binding IsProtrude}" Canvas.Left="36" Canvas.Top="62"></CheckBox>

            <CheckBox Content="Cross" IsChecked="{Binding IsCross}" Canvas.Left="36" Canvas.Top="90"></CheckBox>


            <Button Content="Place" Width="100" Height="35"   Command="{Binding PlaceCommand}" Canvas.Left="10" Margin="0" Canvas.Top="136"></Button>
            <Button Content="Remove" Width="100" Height="35"   Command="{Binding RemoveCommand}" Canvas.Left="141" Margin="0" Canvas.Top="136"></Button>
 

            <ItemsControl Width="300" Height="300" Background="LightGray" Canvas.Left="576" ItemsSource="{Binding WaferList}" ItemTemplate="{DynamicResource DataTemplate1}" >

            </ItemsControl>

            <Button Content="SetAll" Width="100" Height="35"   Command="{Binding SetAllCommand}" Canvas.Left="513" Margin="0" Canvas.Top="305"></Button>
            <Button Content="Random" Width="100" Height="35"   Command="{Binding RandomCommand}" Canvas.Left="646" Margin="0" Canvas.Top="305"></Button>
            <Button Content="Clear" Width="100" Height="35"   Command="{Binding ClearCommand}" Canvas.Left="766" Margin="0" Canvas.Top="305"></Button>

            <Button Content="Clear Log" Width="80" Height="25"   Command="{Binding ClearLogCommand}" Canvas.Left="-405" Canvas.Top="287" Margin="0"></Button>

        </Canvas>



        <!--<DataGrid x:Name="_simaulatorItemGrid" CanUserAddRows="False" ItemsSource="{Binding IOSimulatorItemViewModelConfig.IOSimulatorItemList}" Grid.Row="2" AutoGenerateColumns="False" SelectionMode="Single" >
            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Setter Property="ToolTipService.ToolTip" Value="{Binding RelativeSource={RelativeSource Mode=Self},Path=Content.Text}" />
                </Style>
            </DataGrid.CellStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Command" Width="250" Binding="{Binding SourceCommandName, Mode=OneWay}"/>
                <DataGridTextColumn Header="Content" Width="*" Binding="{Binding CommandContent, Mode=OneWay}"/>
                <DataGridTextColumn Header="Recieved Time" Width="200" Binding="{Binding CommandRecievedTime, Mode=OneWay}"/>
                <DataGridTextColumn Header="Resonse" Width="500" Binding="{Binding Response, UpdateSourceTrigger=LostFocus, Mode=TwoWay}"/>
                <DataGridTemplateColumn Header="Manual Reply" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button x:Name="BtnReply" Content="Reply" Height="20"
                                    Width="50" Click="BtnReply_Click" IsEnabled="{Binding IsManualReplyEnable}"
                                    HorizontalContentAlignment="Center" VerticalContentAlignment="Center">
                            </Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>-->

        <!--IsEnabled="{Binding IsManualReplyEnable}"-->

        <DataGrid Grid.Row="2" FontSize="16" AutoGenerateColumns="False" CanUserAddRows="False" CanUserResizeRows="False" CanUserSortColumns="False" ItemsSource="{Binding TransactionLogItems}" 
                      ScrollViewer.CanContentScroll="True" 
                      ScrollViewer.VerticalScrollBarVisibility="Auto"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto"
                      Width="1800" Height="320" VerticalAlignment="Top">
            <DataGrid.Columns>

                <DataGridTextColumn Header="Time" Width="160" IsReadOnly="True" Binding="{Binding OccurTime, UpdateSourceTrigger=PropertyChanged}"/>

                <DataGridTextColumn Header="Incoming" Width="810" IsReadOnly="True"  Binding="{Binding Incoming, UpdateSourceTrigger=PropertyChanged}">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextWrapping" Value="Wrap"/>
                            <Setter Property="Height" Value="auto"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>


                <DataGridTextColumn Header="Outgoing" Width="810" IsReadOnly="True"  Binding="{Binding Outgoing, UpdateSourceTrigger=PropertyChanged}">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextWrapping" Value="Wrap"/>
                            <Setter Property="Height" Value="auto"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
