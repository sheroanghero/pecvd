<UserControl x:Class="MECF.Framework.Simulator.Core.Chillers.SimChillerSmcHRSView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:deviceControl="clr-namespace:MECF.Framework.UI.Core.DeviceControl;assembly=MECF.Framework.UI.Core"
             xmlns:commons="clr-namespace:MECF.Framework.Simulator.Core.Commons"
             mc:Ignorable="d" 
             d:DesignHeight="900"  d:DesignWidth="900">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"></RowDefinition>
            <RowDefinition Height="150"></RowDefinition>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition Height="400"></RowDefinition>
        </Grid.RowDefinitions>

        <commons:SerialPortTitleView Grid.Row="0"></commons:SerialPortTitleView>

        <Canvas  Grid.Row="1"   Width="900">
            <CheckBox Content="No Response" IsChecked="{Binding IsNoResponse}" Canvas.Left="36" Canvas.Top="31"></CheckBox>

            <CheckBox Content="Remote Mode" IsChecked="{Binding IsRemote}" Canvas.Left="172" Canvas.Top="34"></CheckBox>

            <CheckBox Content="Run" IsChecked="{Binding IsRun}" Canvas.Left="172" Canvas.Top="65"></CheckBox>

            <CheckBox Content="Fault Flag" IsChecked="{Binding IsFault}" Canvas.Left="172" Canvas.Top="96"></CheckBox>

            <CheckBox Content="Alarm Flag" IsChecked="{Binding IsAlarm}" Canvas.Left="172" Canvas.Top="126"></CheckBox>

            <TextBlock Text="Temp PV:" Canvas.Left="368" Canvas.Top="34"></TextBlock>
            <TextBox IsEnabled="False" Text="{Binding TempFeedback }"   Canvas.Left="440" Canvas.Top="22" Margin="0" Width="148" Height="36"></TextBox>

            <TextBlock Text="Temp SV:" Canvas.Left="368" Canvas.Top="77"></TextBlock>
            <TextBox  Text="{Binding TempSetPoint}"   Canvas.Left="441" Canvas.Top="65" Margin="0" Width="147" Height="36"></TextBox>

            <Button Content="Set" Width="100" Height="35"   Command="{Binding SetTempCommand}" Canvas.Left="608" Canvas.Top="65"></Button>

        </Canvas>


        <StackPanel Grid.Row="2" Orientation="Horizontal" Width="900">
            <Button Content="Clear Log" Width="100" Height="35"   Command="{Binding ClearLogCommand}"></Button>

        </StackPanel>


        <DataGrid Grid.Row="3" FontSize="16" AutoGenerateColumns="False" CanUserAddRows="False" CanUserResizeRows="False" CanUserSortColumns="False" ItemsSource="{Binding TransactionLogItems}" 
                      ScrollViewer.CanContentScroll="True" 
                      ScrollViewer.VerticalScrollBarVisibility="Auto"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto"
                      Width="900" Height="380" VerticalAlignment="Top">
            <DataGrid.Columns>

                <DataGridTextColumn Header="Time" Width="160" IsReadOnly="True" Binding="{Binding OccurTime, UpdateSourceTrigger=PropertyChanged}"/>

                <DataGridTextColumn Header="Incoming" Width="350" IsReadOnly="True"  Binding="{Binding Incoming, UpdateSourceTrigger=PropertyChanged}">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextWrapping" Value="Wrap"/>
                            <Setter Property="Height" Value="auto"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>


                <DataGridTextColumn Header="Outgoing" Width="350" IsReadOnly="True"  Binding="{Binding Outgoing, UpdateSourceTrigger=PropertyChanged}">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextWrapping" Value="Wrap"/>
                            <Setter Property="Height" Value="auto"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

            </DataGrid.Columns>
        </DataGrid>
    </Grid>

</UserControl>
