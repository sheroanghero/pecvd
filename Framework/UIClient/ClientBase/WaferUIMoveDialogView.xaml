<UserControl x:Class="MECF.Framework.UI.Client.ClientBase.WaferUIMoveDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Background="Transparent">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
            <RowDefinition Height="10"/>
        </Grid.RowDefinitions>
        <Grid.Effect>
            <DropShadowEffect BlurRadius="6" ShadowDepth="6" Opacity="0.6"/>
        </Grid.Effect>
        <Grid Width="500" Height="140" Margin="0,0,10,0" Grid.Row="1">
            <Border x:Name="DialogWindow" BorderThickness="1,1,0,0" BorderBrush="{DynamicResource Color_BD_Dialog}" Background="{DynamicResource Color_BG_Dialog}" >
                <Border BorderBrush="{DynamicResource Color_BG_Dialog_Shadow}" BorderThickness="0,0,1,1">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="50"/>
                        </Grid.RowDefinitions>
                        <Rectangle Margin="5" Grid.RowSpan="3" Fill="{DynamicResource Color_BG_Dialog_Inner}"/>
                        <Grid  Margin="20,0,0,0" VerticalAlignment="Top" HorizontalAlignment="Left">
                            <Grid x:Name="Information" Width="40" Height="40">
                                <Grid Width="30" Height="30">
                                    <Ellipse Fill="#FF00628B">
                                        <Ellipse.Effect>
                                            <DropShadowEffect Color="White" ShadowDepth="0" BlurRadius="10"/>
                                        </Ellipse.Effect>
                                    </Ellipse>
                                    <Ellipse Margin="3,3,3,1.5">
                                        <Ellipse.Effect>
                                            <BlurEffect Radius="10"/>
                                        </Ellipse.Effect>
                                        <Ellipse.Fill>
                                            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                                                <GradientStop Color="#FF007BDA" Offset="0"/>
                                                <GradientStop Color="#FF68C6D8" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Ellipse.Fill>
                                    </Ellipse>
                                    <Ellipse Margin="5,2.5,5,12">
                                        <Ellipse.Fill>
                                            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                                                <GradientStop Color="White" Offset="0.2"/>
                                                <GradientStop Offset="1"/>
                                            </LinearGradientBrush>
                                        </Ellipse.Fill>
                                    </Ellipse>
                                    <TextBlock TextWrapping="Wrap" Text="i" FontSize="26.667" Foreground="#FF002368" FontFamily="Monotype Corsiva" Margin="9.5,2.5,6.5,-2.5" FontWeight="Bold">
                                        <TextBlock.Effect>
                                            <DropShadowEffect ShadowDepth="0" Color="White"/>
                                        </TextBlock.Effect>
                                    </TextBlock>
                                </Grid>
                                <!--<Grid.Style>
                                            <Style>
                                                <Setter Property="Grid.Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding DialogType}" Value="INFO">
                                                        <Setter Property="Grid.Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Grid.Style>-->
                            </Grid>
                        </Grid>
                        <TextBlock Grid.Row="0" Text="{Binding ConfirmText}" Margin="80,10,0,0" TextWrapping="Wrap" FontFamily="Arial" FontSize="14" Foreground="{DynamicResource FG_Black}" HorizontalAlignment="Left" VerticalAlignment="Top"></TextBlock>
                        <StackPanel Grid.Row="1" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">


      

                            <StackPanel Margin="5" Orientation="Horizontal" Width="400"  Height="34">

                                <Label Content="Move To Module" />

                                <ComboBox x:Name="cbModules" Margin="0,0,10,0"  Height="25" Width="90" ItemsSource="{Binding Modules}"  SelectedItem="{Binding SelectedModule}" />

                                <ComboBox x:Name="cbSlots" Margin="0,0,10,0"  Height="25" Width="90" ItemsSource="{Binding SelectedModuleSlots}"  SelectedItem="{Binding SelectedSlot}" />

                                

                                <Button Content="Move" Command="{Binding TransferCommand}" HorizontalAlignment="Right" Height="25" Width="80"  />


                            </StackPanel>

                      

                     


                        </StackPanel>
                        <StackPanel Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Horizontal" Margin="0,0,0,20">

                            <Button Content="Cancel" Command="{Binding CancelCommand}" HorizontalAlignment="Left" Height="25" Width="80" Margin="5,0,0,0" />
                        </StackPanel>
                    </Grid>
                </Border>
            </Border>
        </Grid>
        <Path Data="M8,0 L8,4 4,0" Fill="{DynamicResource Color_BG_Dialog}" HorizontalAlignment="Right" Margin="0,-2,40,0" Stretch="Fill" Width="8" Grid.Row="2" Stroke="{DynamicResource Color_BG_Dialog_Shadow}"/>
    </Grid>
</UserControl>
