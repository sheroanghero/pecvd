<UserControl x:Class="MECF.Framework.UI.Client.ClientBase.WaferTransferDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Background="Transparent">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
            <RowDefinition Height="10"/>
        </Grid.RowDefinitions>
        <Grid.Effect>
            <DropShadowEffect BlurRadius="6" ShadowDepth="6" Opacity="0.6"/>
        </Grid.Effect>
        <Grid Width="400" Height="230" Margin="0,0,10,0" Grid.Row="1">
            <Border x:Name="DialogWindow" BorderThickness="1,1,0,0" BorderBrush="{DynamicResource Color_BD_Dialog}" Background="{DynamicResource Color_BG_Dialog}" >
                <Border BorderBrush="{DynamicResource Color_BG_Dialog_Shadow}" BorderThickness="0,0,1,1">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition />
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Rectangle Margin="5" Grid.RowSpan="2" Fill="{DynamicResource Color_BG_Dialog_Inner}"/>
                        <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" CanContentScroll="True" Margin="20,20,20,0">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="40"/>
                                    <ColumnDefinition/>
                                </Grid.ColumnDefinitions>
                                <Grid VerticalAlignment="Top">
                                    <Grid x:Name="Information" Width="40" Height="40">
                                        <Grid Width="30" Height="30">
                                            <Ellipse Fill="#FF00628B">
                                                <Ellipse.Effect>
                                                    <DropShadowEffect Color="White" ShadowDepth="0" BlurRadius="10"/>
                                                </Ellipse.Effect>
                                            </Ellipse>
                                            <Ellipse Margin="3,3,3,1.5">
                                                <Ellipse.Effect>
                                                    <BlurEffect Radius="10"/>
                                                </Ellipse.Effect>
                                                <Ellipse.Fill>
                                                    <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                                                        <GradientStop Color="#FF007BDA" Offset="0"/>
                                                        <GradientStop Color="#FF68C6D8" Offset="1"/>
                                                    </LinearGradientBrush>
                                                </Ellipse.Fill>
                                            </Ellipse>
                                            <Ellipse Margin="5,2.5,5,12">
                                                <Ellipse.Fill>
                                                    <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                                                        <GradientStop Color="White" Offset="0.2"/>
                                                        <GradientStop Offset="1"/>
                                                    </LinearGradientBrush>
                                                </Ellipse.Fill>
                                            </Ellipse>
                                            <TextBlock TextWrapping="Wrap" Text="i" FontSize="26.667" Foreground="#FF002368" FontFamily="Monotype Corsiva" Margin="9.5,2.5,6.5,-2.5" FontWeight="Bold">
                                                <TextBlock.Effect>
                                                    <DropShadowEffect ShadowDepth="0" Color="White"/>
                                                </TextBlock.Effect>
                                            </TextBlock>
                                        </Grid>
                                        <!--<Grid.Style>
                                            <Style>
                                                <Setter Property="Grid.Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding DialogType}" Value="INFO">
                                                        <Setter Property="Grid.Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Grid.Style>-->
                                    </Grid>
                                </Grid>
                                <TextBlock Grid.Column="1" Text="{Binding ConfirmText}" Margin="0,10,0,0" TextWrapping="Wrap" FontFamily="Arial" FontSize="14" Foreground="{DynamicResource FG_Black}" HorizontalAlignment="Left" VerticalAlignment="Top"></TextBlock>
                                <StackPanel Grid.Column="1" Orientation="Vertical" VerticalAlignment="Bottom">
                                    <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="0,0,0,10" Visibility="{Binding AlignerVisibility}">
                                        <CheckBox IsEnabled="{Binding DisplayPassAlignerCondition }" IsChecked="{Binding Conditions.IsPassAligner}" HorizontalAlignment="Left"
                                                VerticalAlignment="Bottom" Width="120">Auto align</CheckBox>
                                        <TextBlock Text="Aligner Angle" VerticalAlignment="Bottom" Width="80" Visibility="Hidden"/>
                                        <TextBox IsEnabled="{Binding DisplayPassAlignerCondition }" Text="{Binding Conditions.AlignerAngle, Mode=TwoWay}" Width="50" Visibility="Hidden"/>
                                        <TextBlock Text="" VerticalAlignment="Bottom" Margin="5,0,0,0"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="0,0,0,10" Visibility="{Binding CoolingVisibility}">
                                        <CheckBox IsEnabled="{Binding DisplayPassCoolingCondition }" IsChecked="{Binding Conditions.IsPassCooling}" HorizontalAlignment="Left"
                                                VerticalAlignment="Bottom" Width="120" Content="Auto Cooling"></CheckBox>
                                        <TextBlock Text="Cooling time" VerticalAlignment="Bottom" Width="80"/>
                                        <TextBox IsEnabled="{Binding DisplayPassCoolingCondition }" Text="{Binding Conditions.CoolingTime, Mode=TwoWay}" Width="50" />
                                        <TextBlock Text="s" VerticalAlignment="Bottom" Margin="5,0,0,0"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="0,0,0,10" Visibility="{Binding BladeVisibility}">
                                        <Label Content="Blade:" IsEnabled="{Binding DisplayBladeCondition }" Width="50"/>
                                        <WrapPanel VerticalAlignment="Center" IsEnabled="{Binding DisplayBladeCondition }">
                                            <RadioButton  Content="Blade1" IsChecked="{Binding IsBlade1, Mode=TwoWay}" />
                                            <RadioButton  Margin="8,0" IsChecked="{Binding IsBlade2, Mode=TwoWay}" Content="Blade2" />
                                        </WrapPanel>
                                    </StackPanel>
                                </StackPanel>

                            </Grid>
                        </ScrollViewer>
                        <StackPanel Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Horizontal" Margin="0,0,0,20">
                            <Button Content="Transfer" Command="{Binding TransferCommand}" HorizontalAlignment="Left" Height="25" Width="80"  />
                            <Button Content="Cancel" Command="{Binding CancelCommand}" HorizontalAlignment="Left" Height="25" Width="80" Margin="5,0,0,0" />
                        </StackPanel>
                    </Grid>
                </Border>
            </Border>
        </Grid>
        <Path Data="M8,0 L8,4 4,0" Fill="{DynamicResource Color_BG_Dialog}" HorizontalAlignment="Right" Margin="0,-2,40,0" Stretch="Fill" Width="8" Grid.Row="2" Stroke="{DynamicResource Color_BG_Dialog_Shadow}"/>
    </Grid>
</UserControl>
