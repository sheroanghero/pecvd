<UserControl x:Class="MECF.Framework.UI.Client.CenterViews.Editors.Recipe.ConfigCompareView"
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:wfi ="clr-namespace:System.Windows.Forms.Integration;assembly=WindowsFormsIntegration" 
             xmlns:wf ="clr-namespace:System.Windows.Forms;assembly=System.Windows.Forms"
             xmlns:interop="clr-namespace:Microsoft.DwayneNeed.Interop;assembly=Microsoft.DwayneNeed"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
             xmlns:editors="clr-namespace:MECF.Framework.UI.Client.CenterViews.Editors"
             xmlns:controls="clr-namespace:OpenSEMI.Ctrlib.Controls"
             xmlns:micro="clr-namespace:Caliburn.Micro"
             xmlns:dgExtension="clr-namespace:RecipeEditorLib.DGExtension"
             mc:Ignorable="d" FontFamily="Segoe"
             d:DesignHeight="900" d:DesignWidth="1500">
    <Grid Margin="-5" >
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{DynamicResource Table_BG_Content}" BorderBrush="{DynamicResource Table_BD}" CornerRadius="5,5,0,0" BorderThickness="1">
            <Grid >

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border  IsEnabled="{Binding Path=IsStepModel}"  BorderBrush="{DynamicResource Table_BD}" Margin="5" Padding="3"  BorderThickness="1"  Grid.Column="0" HorizontalAlignment="Left" Width="180">
                    <StackPanel Orientation="Vertical" >
                        <TextBlock VerticalAlignment="Center" FontSize="23"  Text="Config Compare" TextWrapping="Wrap" />
                    </StackPanel>
                </Border>
                <!--<Border  IsEnabled="{Binding Path=IsStepModel}"  BorderBrush="{DynamicResource Table_BD}" Margin="5" Padding="3"  BorderThickness="1"  Grid.Column="0" HorizontalAlignment="Left" Width="180">
                    <StackPanel Orientation="Vertical" >
                        <CheckBox Content="Show Differing Steps"  VerticalContentAlignment="Center" IsChecked="{Binding Path=IsShowDiffSteps, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                        <CheckBox Content="Show All Steps" VerticalContentAlignment="Center" IsChecked="{Binding Path=IsShowAllSteps, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                </Border>

                <Border  IsEnabled="{Binding Path=IsStepModel}"  BorderBrush="{DynamicResource Table_BD}" Margin="5" Padding="3"  BorderThickness="1"  Grid.Column="1" HorizontalAlignment="Left" Width="180">
                    <StackPanel Orientation="Vertical" Margin="0,0,0,0">
                        <CheckBox  VerticalContentAlignment="Center"  IsChecked="{Binding Path=IsShowDiffParams, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Content="Show Differing Parameters"/>
                        <CheckBox  VerticalContentAlignment="Center" Content="Show All Parameters" IsChecked="{Binding Path=IsShowAllParams, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                </Border>

                <Border  IsEnabled="{Binding Path=IsStepModel}"  BorderBrush="{DynamicResource Table_BD}" Margin="5" Padding="3"  BorderThickness="1"  Grid.Column="2" HorizontalAlignment="Left" Width="180">
                    <StackPanel Orientation="Vertical" Margin="0,0,0,0">
                        <CheckBox Content="Compare By Step #" VerticalContentAlignment="Center" IsChecked="{Binding Path=IsCompareByStep, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                        <CheckBox Content="Compare By Step Name" VerticalContentAlignment="Center" IsChecked="{Binding Path=IsCompareBySName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                </Border>

                <Border    BorderBrush="{DynamicResource Table_BD}" Margin="5" Padding="3"  BorderThickness="1"  Grid.Column="3" HorizontalAlignment="Left" Width="180">
                    <StackPanel Orientation="Vertical" Margin="0,0,0,0">
                        <CheckBox Content="Step Mode" VerticalContentAlignment="Center" IsChecked="{Binding Path=IsStepModel, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                        <CheckBox Content="Whole Mode" VerticalContentAlignment="Center" IsChecked="{Binding Path=IsWholeModel, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                </Border>-->
                <Border   BorderBrush="{DynamicResource Table_BD}" Margin="5" Padding="3"  BorderThickness="1"  Grid.Column="2" HorizontalAlignment="Left" Width="180">
                    <StackPanel Orientation="Vertical" Margin="0,0,0,0">
                        <Button Margin="5,2,0,0" Grid.RowSpan="2" IsEnabled="{Binding CanCompare}" Content="Compare" Grid.Column="3" >
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Click">
                                    <micro:ActionMessage MethodName="Compare" />
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </Button>
                    </StackPanel>
                </Border>
                <Border   BorderBrush="{DynamicResource Table_BD}" Margin="5" Padding="3"  BorderThickness="1"  Grid.Column="1" HorizontalAlignment="Left" Width="180">
                    <Canvas>
                        <Rectangle Width="50" Height="10" Fill="Tomato" Stroke="Black" StrokeThickness="1" Canvas.Top="2"  />
                        <TextBlock Text="Difference"   FontSize="12"   VerticalAlignment="Center" Canvas.Left="55"/>

                        <Rectangle Width="50" Height="10" Fill="Gold" Stroke="Black" StrokeThickness="1" Canvas.Top="18"  />
                        <TextBlock Text="Omit/Skip"   FontSize="12"   VerticalAlignment="Center" Canvas.Left="55" Canvas.Top="16"/>

                    </Canvas>
                </Border>
            </Grid>
        </Border>


        <Grid Grid.Row="1" Margin="0,8,0,0"  >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="200*"/>
                <ColumnDefinition Width="430"/>

                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="200*"/>
                <ColumnDefinition Width="400"/>

            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Padding="5,1,0,1" Background="{DynamicResource Table_BG_Title}" BorderBrush="{DynamicResource Table_BD}" BorderThickness="1,1,1,1">
                <TextBlock VerticalAlignment="Center" FontSize="13" Foreground="{DynamicResource FG_Black}" Text="Config Name" TextWrapping="Wrap" />
            </Border>

            <Border Grid.Column="1"  Padding="5,1" Background="{DynamicResource Table_BG_Content}" BorderBrush="{DynamicResource Table_BD}" BorderThickness="0,1,1,1">
                <TextBlock VerticalAlignment="Center" FontSize="13" Foreground="{DynamicResource FG_Black}" Text="{Binding RecipeA}" TextWrapping="Wrap" HorizontalAlignment="Left"  />
            </Border>

            <StackPanel Grid.Column="2" Orientation="Horizontal" Width="400">
                <Button   Margin="5,0,0,0" Padding="5,1"  Width="80" Height="25"  Content="Select" FontSize="13" HorizontalAlignment="Left">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Click">
                            <micro:ActionMessage MethodName="SelectA" />
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </Button>
                <Button IsEnabled="{Binding EnableButtonRemoveA}"  Margin="5,0,0,0" Padding="5,1"  Width="80" Height="25"  Content="Remove" FontSize="13" HorizontalAlignment="Left">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Click">
                            <micro:ActionMessage MethodName="RemoveA" />
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </Button>

                <Button   Margin="35,0,0,0" Padding="5,1" Visibility="Hidden"  Width="70" Height="25"  Content="Undo" FontSize="13" IsEnabled="{Binding IsSelectButtonEnable}"  HorizontalAlignment="Left">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Click">
                            <micro:ActionMessage MethodName="UndoA" />
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </Button>

                <Button  Margin="15,0,0,0" Padding="5,1" Width="70" Height="25"  Content="Save" FontSize="13" IsEnabled="{Binding IsSelectButtonEnable}" HorizontalAlignment="Left">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Click">
                            <micro:ActionMessage MethodName="SaveA" />
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </Button>

            </StackPanel>




            <Border Grid.Column="3" Padding="5,1,0,1" Background="{DynamicResource Table_BG_Title}" BorderBrush="{DynamicResource Table_BD}" BorderThickness="1,1,1,1">
                <TextBlock VerticalAlignment="Center" FontSize="13" Foreground="{DynamicResource FG_Black}" Text="Config Name" TextWrapping="Wrap" />
            </Border>

            <Border Grid.Column="4"  Padding="5,1" Background="{DynamicResource Table_BG_Content}" BorderBrush="{DynamicResource Table_BD}" BorderThickness="0,1,1,1">
                <TextBlock VerticalAlignment="Center" FontSize="13" Foreground="{DynamicResource FG_Black}" Text="{Binding RecipeB}" TextWrapping="Wrap" HorizontalAlignment="Left"  />
            </Border>
            <StackPanel Grid.Column="5" Orientation="Horizontal" Width="400">
                <Button   Margin="5,0,0,0" Padding="5,1"  Width="80" Height="25"  Content="Select" FontSize="13" HorizontalAlignment="Left">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Click">
                            <micro:ActionMessage MethodName="SelectB" />
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </Button>
                <Button  IsEnabled="{Binding EnableButtonRemoveB}"   Margin="5,0,0,0" Padding="5,1"  Width="80" Height="25"  Content="Remove" FontSize="13" HorizontalAlignment="Left">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Click">
                            <micro:ActionMessage MethodName="RemoveB" />
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </Button>

                <Button   Margin="35,0,0,0" Padding="5,1" Visibility="Hidden"  Width="70" Height="25"  Content="Undo" FontSize="13" IsEnabled="{Binding IsSelectButtonEnable}"  HorizontalAlignment="Left">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Click">
                            <micro:ActionMessage MethodName="UndoB" />
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </Button>

                <Button  Margin="15,0,0,0" Padding="5,1" Width="70" Height="25"  Content="Save" FontSize="13" IsEnabled="{Binding IsSelectButtonEnable}" HorizontalAlignment="Left">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Click">
                            <micro:ActionMessage MethodName="SaveB" />
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </Button>

            </StackPanel>
        </Grid>

        <Grid Grid.Row="2"  Visibility="Visible" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100*"/>
                <ColumnDefinition Width="100*"/>
            </Grid.ColumnDefinitions>

            <Border Margin="0,1,15,0" Grid.Column="0" HorizontalAlignment="Stretch" Background="{DynamicResource Table_BG_Content}" BorderBrush="{DynamicResource Table_BD}" CornerRadius=" 0" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="20"/>
                        <RowDefinition Height="20"/>
                        <RowDefinition Height="774*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Column="0" Grid.Row="0" Grid.RowSpan="2"  >
                        <Grid.RowDefinitions>
                            <RowDefinition Height="20"/>
                            <RowDefinition Height="20"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="50"/>
                            <ColumnDefinition Width="50"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="100"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Margin="10,5,0,0" Grid.Column="0" Text="Line :"></TextBlock>
                        <TextBlock Margin="5,2,0,0" Grid.Column="1"  FontWeight="Bold" FontSize="15" Text="{Binding LineSelectionA.LineNumber}"></TextBlock>
                        <TextBox Margin="5,2,0,0" Grid.Column="2" Grid.RowSpan="2"  FontWeight="Bold" FontSize="15" Height="40" TextWrapping="Wrap" Text="{Binding LineSelectionA.LineText,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}"></TextBox>
                        <Button Margin="5,2,0,0" Grid.RowSpan="2" Content="Modify" Grid.Column="3" >
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Click">
                                    <micro:ActionMessage MethodName="SaveLineA" />
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </Button>
                    </Grid>

                    <DataGrid x:Name="WholeGridA" Grid.Column="0" Grid.Row="2" AutoGenerateColumns="False" Margin="5"  
                          Background="White" ItemsSource="{Binding WholeListA, Mode=OneWay}" 
                          SelectionMode="Single" SelectionUnit="FullRow"
                              VirtualizingPanel.IsContainerVirtualizable="True"
                          VirtualizingPanel.IsVirtualizing="True"
                          VirtualizingPanel.VirtualizationMode="Standard"
                          EnableColumnVirtualization="True"
                          EnableRowVirtualization="True" 
                          FontFamily="Segoe" FontSize="13"                
                          BorderThickness="0.1"  SelectedItem="{Binding LineSelectionA}"
                          CanUserDeleteRows="False" CanUserAddRows="False" IsReadOnly="True" Grid.RowSpan="2" >
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="SelectionChanged">
                                <micro:ActionMessage MethodName="WholeGridSelectionChangedA">
                                </micro:ActionMessage>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Width="100" Header="Copy To Left">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="&gt;"  Visibility="{Binding CopyVisibility}" Width="60" Height="20" Padding="0,-3" FontSize="20">
                                            <i:Interaction.Triggers>
                                                <i:EventTrigger EventName="Click">
                                                    <micro:ActionMessage MethodName="LineCopyToRight">
                                                        <micro:Parameter Value="{Binding }" />
                                                    </micro:ActionMessage>
                                                </i:EventTrigger>
                                            </i:Interaction.Triggers>
                                        </Button>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="Line #"  Width="55" CanUserSort="True" IsReadOnly="True">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding LineNumber}" Margin="5,0"  FontSize="14" Foreground="{DynamicResource FG_Black}" HorizontalAlignment="Left" VerticalAlignment="Center" ToolTip="{Binding RelativeSource={RelativeSource Self}, Path=Text}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                                <DataGridTemplateColumn.CellStyle>
                                    <Style TargetType="DataGridCell">
                                        <Setter Property="Background" Value="{Binding Path=Background}" />
                                    </Style>
                                </DataGridTemplateColumn.CellStyle>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="Line Text" CanUserSort="True" IsReadOnly="True">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding LineText}" Margin="5,0"  FontSize="14" Foreground="{DynamicResource FG_Black}" HorizontalAlignment="Left" VerticalAlignment="Center" ToolTip="{Binding RelativeSource={RelativeSource Self}, Path=Text}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                                <DataGridTemplateColumn.CellStyle>
                                    <Style TargetType="DataGridCell">
                                        <Setter Property="Background" Value="{Binding Path=Background}" />
                                    </Style>
                                </DataGridTemplateColumn.CellStyle>
                            </DataGridTemplateColumn>


                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>

            </Border>

            <GridSplitter Grid.Column="1" HorizontalAlignment="Left"  Cursor="SizeWE"  Width="10"  >
                <GridSplitter.Resources>
                    <Style TargetType="{x:Type GridSplitter}">
                        <Setter Property="PreviewStyle">
                            <Setter.Value>
                                <Style>
                                    <Setter Property="Control.Template">
                                        <Setter.Value>
                                            <ControlTemplate>
                                                <Rectangle Fill="#80000000"/>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Setter.Value>
                        </Setter>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type GridSplitter}">
                                    <Border 
                                                                            BorderThickness="{TemplateBinding BorderThickness}"/>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </GridSplitter.Resources>
                <GridSplitter.Template>
                    <ControlTemplate TargetType="{x:Type GridSplitter}">
                        <Grid VerticalAlignment="Stretch">
                            <Button  Background="#DDE0E3"  VerticalAlignment="Stretch" VerticalContentAlignment="Center" Content="⁞" BorderBrush="Transparent" BorderThickness="1"  Margin="-0.3,0,0,0"/>
                            <Rectangle  Fill="Transparent" VerticalAlignment="Stretch"/>
                        </Grid>
                    </ControlTemplate>
                </GridSplitter.Template>
            </GridSplitter>

            <Border Margin="15,1,0,0" Grid.Column="1" HorizontalAlignment="Stretch" Background="{DynamicResource Table_BG_Content}" BorderBrush="{DynamicResource Table_BD}" CornerRadius=" 0" BorderThickness="1" >
                <Grid Grid.Row="2"  >
                    <Grid.RowDefinitions>
                        <RowDefinition Height="20"/>
                        <RowDefinition Height="20"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>


                    <Grid Grid.Column="0" Grid.Row="0" Grid.RowSpan="2" >
                        <Grid.RowDefinitions>
                            <RowDefinition Height="20"/>
                            <RowDefinition Height="20"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="50"/>
                            <ColumnDefinition Width="50"/>
                            <ColumnDefinition Width="100*"/>
                            <ColumnDefinition Width="100"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Margin="10,5,0,0" Grid.Column="0" Text="Line:"></TextBlock>
                        <TextBlock Margin="5,2,0,0" Grid.Column="1" FontWeight="Bold" FontSize="15" Text="{Binding LineSelectionB.LineNumber}"></TextBlock>
                        <TextBox Margin="5,2,0,0" Grid.Column="2" Grid.RowSpan="2"  FontWeight="Bold" FontSize="15" Height="40" TextWrapping="Wrap"  Text="{Binding LineSelectionB.LineText,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}"></TextBox>
                        <Button Margin="5,2,0,0" Grid.RowSpan="2" Grid.Column="3" Content="Modify" >
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Click">
                                    <micro:ActionMessage MethodName="SaveLineB" />
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </Button>
                    </Grid>


                    <DataGrid x:Name="WholeGridB" Grid.Column="0" Grid.Row="2" AutoGenerateColumns="False" Margin="5"  
                          Background="White" ItemsSource="{Binding WholeListB, Mode=OneWay}" 
                          SelectionMode="Single" SelectionUnit="FullRow"
                          VirtualizingPanel.IsContainerVirtualizable="True"
                          VirtualizingPanel.IsVirtualizing="True"
                          VirtualizingPanel.VirtualizationMode="Recycling"
                          EnableColumnVirtualization="True"
                          EnableRowVirtualization="True" 
                             
                          FontFamily="Segoe" FontSize="13"                     
                          BorderThickness="0.1"  SelectedItem="{Binding LineSelectionB}"
                          CanUserDeleteRows="False" CanUserAddRows="False" IsReadOnly="True" >
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="SelectionChanged">
                                <micro:ActionMessage MethodName="WholeGridSelectionChangedB">
                                </micro:ActionMessage>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Width="100" Header="Copy To Left">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="&lt;"  Visibility="{Binding CopyVisibility}" Width="60" Height="20" Padding="0,-3" FontSize="20">
                                            <i:Interaction.Triggers>
                                                <i:EventTrigger EventName="Click">
                                                    <micro:ActionMessage MethodName="LineCopyToLeft">
                                                        <micro:Parameter Value="{Binding }" />
                                                    </micro:ActionMessage>
                                                </i:EventTrigger>
                                            </i:Interaction.Triggers>
                                        </Button>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>



                            <DataGridTemplateColumn Header="Line #"  Width="55" CanUserSort="True" IsReadOnly="True">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding LineNumber}" Margin="5,0"  FontSize="14" Foreground="{DynamicResource FG_Black}" HorizontalAlignment="Left" VerticalAlignment="Center" ToolTip="{Binding RelativeSource={RelativeSource Self}, Path=Text}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                                <DataGridTemplateColumn.CellStyle>
                                    <Style TargetType="DataGridCell">
                                        <Setter Property="Background" Value="{Binding Path=Background}" />
                                    </Style>
                                </DataGridTemplateColumn.CellStyle>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="Line Text" CanUserSort="True" IsReadOnly="True">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding LineText}" Margin="5,0"  FontSize="14" Foreground="{DynamicResource FG_Black}" HorizontalAlignment="Left" VerticalAlignment="Center" ToolTip="{Binding RelativeSource={RelativeSource Self}, Path=Text}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                                <DataGridTemplateColumn.CellStyle>
                                    <Style TargetType="DataGridCell">
                                        <Setter Property="Background" Value="{Binding Path=Background}" />
                                    </Style>
                                </DataGridTemplateColumn.CellStyle>
                            </DataGridTemplateColumn>


                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

        </Grid>

        <!--<Grid Grid.Row="2" Visibility="{Binding StepVisibility}" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100*"/>
                <ColumnDefinition Width="100*"/>
            </Grid.ColumnDefinitions>

            <Border Margin="0,1,15,0" Grid.Column="0" HorizontalAlignment="Stretch" Background="{DynamicResource Table_BG_Content}" BorderBrush="{DynamicResource Table_BD}" CornerRadius=" 0" BorderThickness="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100*"/>
                        <ColumnDefinition Width="100*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="20"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Column="0" Grid.Row="0"  Orientation="Horizontal">
                        <TextBlock Margin="20,5,0,0" Text="Step:"></TextBlock>
                        <TextBlock Margin="10,2,0,0" FontWeight="Bold" FontSize="15" Text="{Binding StepSelectionA.StepNumber}"></TextBlock>
                        <TextBlock Margin="10,2,0,0" FontWeight="Bold" FontSize="15" Text="{Binding StepSelectionA.StepName}"></TextBlock>
                    </StackPanel>
                    <DataGrid x:Name="StepGridA" Grid.Column="0" Grid.Row="1" AutoGenerateColumns="False" Margin="5"  
                              Background="White" ItemsSource="{Binding StepListA, Mode=OneWay}" 
                              SelectionMode="Single" SelectionUnit="FullRow"
                              FontFamily="Segoe" FontSize="13"                     
                              BorderThickness="0.1" SelectedItem="{Binding StepSelectionA}"
                              CanUserDeleteRows="False" CanUserAddRows="False" IsReadOnly="True" >
                        <DataGrid.RowStyle>
                            <Style TargetType="{x:Type DataGridRow}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsHidden}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="SelectionChanged">
                                <micro:ActionMessage MethodName="StepGridSelectionChangedA">
                                </micro:ActionMessage>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                        <DataGrid.Columns>

                            <DataGridTemplateColumn Width="90" Header="Delete">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="x"  Visibility="{Binding DeleteVisibility}" Width="60" Height="20" Padding="0,-3" FontSize="20">
                                            <i:Interaction.Triggers>
                                                <i:EventTrigger EventName="Click">
                                                    <micro:ActionMessage MethodName="LeftDelete">
                                                        <micro:Parameter Value="{Binding }" />
                                                    </micro:ActionMessage>
                                                </i:EventTrigger>
                                            </i:Interaction.Triggers>
                                        </Button>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Width="90" Header="Copy To Right">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="&gt;" Visibility="{Binding CopyVisibility}" Width="60" Height="20" Padding="0,-3" FontSize="20">
                                            <i:Interaction.Triggers>
                                                <i:EventTrigger EventName="Click">
                                                    <micro:ActionMessage MethodName="StepCopyToRight">
                                                        <micro:Parameter Value="{Binding }" />
                                                    </micro:ActionMessage>
                                                </i:EventTrigger>
                                            </i:Interaction.Triggers>
                                        </Button>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>



                            <DataGridTemplateColumn Header="Step #"  Width="55" CanUserSort="True" IsReadOnly="True">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding StepNumber}" Margin="5,0"  FontSize="14" Foreground="{DynamicResource FG_Black}" HorizontalAlignment="Left" VerticalAlignment="Center" ToolTip="{Binding RelativeSource={RelativeSource Self}, Path=Text}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                                <DataGridTemplateColumn.CellStyle>
                                    <Style TargetType="DataGridCell">
                                        <Setter Property="Background" Value="{Binding Path=Background}" />
                                    </Style>
                                </DataGridTemplateColumn.CellStyle>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="Step Name" Width="*" CanUserSort="True" IsReadOnly="True">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding StepName}" Margin="5,0"  FontSize="14" Foreground="{DynamicResource FG_Black}" HorizontalAlignment="Left" VerticalAlignment="Center" ToolTip="{Binding RelativeSource={RelativeSource Self}, Path=Text}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                                <DataGridTemplateColumn.CellStyle>
                                    <Style TargetType="DataGridCell">
                                        <Setter Property="Background" Value="{Binding Path=Background}" />
                                    </Style>
                                </DataGridTemplateColumn.CellStyle>
                            </DataGridTemplateColumn>


                        </DataGrid.Columns>
                    </DataGrid>

                    <StackPanel Grid.Column="1" Grid.Row="0"  Orientation="Horizontal">
                        <TextBlock Margin="20,5,0,0" Text="Step Parameter:"></TextBlock>
                        <TextBlock Margin="10,2,0,0" FontWeight="Bold" FontSize="15" Text="{Binding ParamSelectionA.ParamName}"></TextBlock>
                        <TextBlock Margin="10,2,0,0" FontWeight="Bold" FontSize="15" Text="{Binding ParamSelectionA.ParamValue}"></TextBlock>
                    </StackPanel>
                    <DataGrid x:Name="ParamGridA" Grid.Column="1" Grid.Row="1" AutoGenerateColumns="False" Margin="5"  
                              Background="White" ItemsSource="{Binding ParamListA, Mode=OneWay}" 
                              SelectionMode="Single" SelectionUnit="FullRow"
                              FontFamily="Segoe" FontSize="13"                     
                              BorderThickness="0.1"  SelectedItem="{Binding ParamSelectionA}"
                              CanUserDeleteRows="False" CanUserAddRows="False" IsReadOnly="True" >
                        <DataGrid.RowStyle>
                            <Style TargetType="{x:Type DataGridRow}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsHidden}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="SelectionChanged">
                                <micro:ActionMessage MethodName="ParamGridSelectionChangedA">
                                </micro:ActionMessage>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                        <DataGrid.Columns>

                            <DataGridTemplateColumn Width="100" Header="Copy To Right">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="&gt;" Visibility="{Binding CopyVisibility}" Width="60" Height="25" Padding="0" FontSize="20">
                                            <i:Interaction.Triggers>
                                                <i:EventTrigger EventName="Click">
                                                    <micro:ActionMessage MethodName="ParamCopyToRight">
                                                        <micro:Parameter Value="{Binding }" />
                                                    </micro:ActionMessage>
                                                </i:EventTrigger>
                                            </i:Interaction.Triggers>
                                        </Button>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="Parameter Name" Width="180"  CanUserSort="True" IsReadOnly="True">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding ParamName}" Margin="5,0"  FontSize="14" Foreground="{DynamicResource FG_Black}" HorizontalAlignment="Left" VerticalAlignment="Center" ToolTip="{Binding RelativeSource={RelativeSource Self}, Path=Text}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                                <DataGridTemplateColumn.CellStyle>
                                    <Style TargetType="DataGridCell">
                                        <Setter Property="Background" Value="{Binding Path=Background}" />
                                    </Style>
                                </DataGridTemplateColumn.CellStyle>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="Parameter Value" Width="*" CanUserSort="True" IsReadOnly="True">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding ParamValue}" Margin="5,0"  FontSize="14" Foreground="{DynamicResource FG_Black}" HorizontalAlignment="Left" VerticalAlignment="Center" ToolTip="{Binding RelativeSource={RelativeSource Self}, Path=Text}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                                <DataGridTemplateColumn.CellStyle>
                                    <Style TargetType="DataGridCell">
                                        <Setter Property="Background" Value="{Binding Path=Background}" />
                                    </Style>
                                </DataGridTemplateColumn.CellStyle>
                            </DataGridTemplateColumn>


                        </DataGrid.Columns>
                    </DataGrid>

                </Grid>

            </Border>

            <GridSplitter Grid.Column="1" HorizontalAlignment="Left"  Cursor="SizeWE"  Width="10"  >
                <GridSplitter.Resources>
                    <Style TargetType="{x:Type GridSplitter}">
                        <Setter Property="PreviewStyle">
                            <Setter.Value>
                                <Style>
                                    <Setter Property="Control.Template">
                                        <Setter.Value>
                                            <ControlTemplate>
                                                <Rectangle Fill="#80000000"/>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Setter.Value>
                        </Setter>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type GridSplitter}">
                                    <Border 
                                                                            BorderThickness="{TemplateBinding BorderThickness}"/>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </GridSplitter.Resources>
                <GridSplitter.Template>
                    <ControlTemplate TargetType="{x:Type GridSplitter}">
                        <Grid VerticalAlignment="Stretch">
                            <Button  Background="#DDE0E3"  VerticalAlignment="Stretch" VerticalContentAlignment="Center" Content="⁞" BorderBrush="Transparent" BorderThickness="1"  Margin="-0.3,0,0,0"/>
                            <Rectangle  Fill="Transparent" VerticalAlignment="Stretch"/>
                        </Grid>
                    </ControlTemplate>
                </GridSplitter.Template>
            </GridSplitter>

            <Border Margin="15,1,0,0" Grid.Column="1" HorizontalAlignment="Stretch" Background="{DynamicResource Table_BG_Content}" BorderBrush="{DynamicResource Table_BD}" CornerRadius=" 0" BorderThickness="1" >
                <Grid Grid.Row="2"  >
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100*"/>
                        <ColumnDefinition Width="100*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="20"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>


                    <StackPanel Grid.Column="0" Grid.Row="0"  Orientation="Horizontal">
                        <TextBlock Margin="20,5,0,0" Text="Step:"></TextBlock>
                        <TextBlock Margin="10,2,0,0" FontWeight="Bold" FontSize="15" Text="{Binding StepSelectionB.StepNumber}"></TextBlock>
                        <TextBlock Margin="10,2,0,0" FontWeight="Bold" FontSize="15" Text="{Binding StepSelectionB.StepName}"></TextBlock>
                    </StackPanel>
                    <DataGrid x:Name="StepGridB" Grid.Column="0" Grid.Row="1" AutoGenerateColumns="False" Margin="5"  
                          Background="White" ItemsSource="{Binding StepListB, Mode=OneWay}" 
                          SelectionMode="Single" SelectionUnit="FullRow"
                          FontFamily="Segoe" FontSize="13"                     
                          BorderThickness="0.1"  SelectedItem="{Binding StepSelectionB}"
                          CanUserDeleteRows="False" CanUserAddRows="False" IsReadOnly="True" >
                        <DataGrid.RowStyle>
                            <Style TargetType="{x:Type DataGridRow}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsHidden}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="SelectionChanged">
                                <micro:ActionMessage MethodName="StepGridSelectionChangedB">
                                </micro:ActionMessage>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Width="90" Header="Delete">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="x"  Visibility="{Binding DeleteVisibility}" Width="60" Height="20" Padding="0,-3" FontSize="20">
                                            <i:Interaction.Triggers>
                                                <i:EventTrigger EventName="Click">
                                                    <micro:ActionMessage MethodName="RightDelete">
                                                        <micro:Parameter Value="{Binding }" />
                                                    </micro:ActionMessage>
                                                </i:EventTrigger>
                                            </i:Interaction.Triggers>
                                        </Button>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Width="90" Header="Copy To Left">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="&lt;"  Visibility="{Binding CopyVisibility}" Width="60" Height="20" Padding="0,-3" FontSize="20">
                                            <i:Interaction.Triggers>
                                                <i:EventTrigger EventName="Click">
                                                    <micro:ActionMessage MethodName="StepCopyToLeft">
                                                        <micro:Parameter Value="{Binding }" />
                                                    </micro:ActionMessage>
                                                </i:EventTrigger>
                                            </i:Interaction.Triggers>
                                        </Button>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>



                            <DataGridTemplateColumn Header="Step #"  Width="55" CanUserSort="True" IsReadOnly="True">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding StepNumber}" Margin="5,0"  FontSize="14" Foreground="{DynamicResource FG_Black}" HorizontalAlignment="Left" VerticalAlignment="Center" ToolTip="{Binding RelativeSource={RelativeSource Self}, Path=Text}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                                <DataGridTemplateColumn.CellStyle>
                                    <Style TargetType="DataGridCell">
                                        <Setter Property="Background" Value="{Binding Path=Background}" />
                                    </Style>
                                </DataGridTemplateColumn.CellStyle>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="Step Name" Width="*" CanUserSort="True" IsReadOnly="True">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding StepName}" Margin="5,0"  FontSize="14" Foreground="{DynamicResource FG_Black}" HorizontalAlignment="Left" VerticalAlignment="Center" ToolTip="{Binding RelativeSource={RelativeSource Self}, Path=Text}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                                <DataGridTemplateColumn.CellStyle>
                                    <Style TargetType="DataGridCell">
                                        <Setter Property="Background" Value="{Binding Path=Background}" />
                                    </Style>
                                </DataGridTemplateColumn.CellStyle>
                            </DataGridTemplateColumn>


                        </DataGrid.Columns>
                    </DataGrid>

                    <StackPanel Grid.Column="1" Grid.Row="0"  Orientation="Horizontal">
                        <TextBlock Margin="20,5,0,0" Text="Step Parameter:"></TextBlock>
                        <TextBlock Margin="10,2,0,0" FontWeight="Bold" FontSize="15" Text="{Binding ParamSelectionB.ParamName}"></TextBlock>
                        <TextBlock Margin="10,2,0,0" FontWeight="Bold" FontSize="15" Text="{Binding ParamSelectionB.ParamValue}"></TextBlock>
                    </StackPanel>
                    <DataGrid x:Name="ParamGridB" Grid.Column="1" Grid.Row="1" AutoGenerateColumns="False" Margin="5"  
                              Background="White" ItemsSource="{Binding ParamListB, Mode=OneWay}" 
                              SelectionMode="Single" SelectionUnit="FullRow"
                              FontFamily="Segoe" FontSize="13"                     
                              BorderThickness="0.1"  SelectedItem="{Binding ParamSelectionB}"
                              CanUserDeleteRows="False" CanUserAddRows="False" IsReadOnly="True" >
                        <DataGrid.RowStyle>
                            <Style TargetType="{x:Type DataGridRow}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsHidden}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="SelectionChanged">
                                <micro:ActionMessage MethodName="ParamGridSelectionChangedB">
                                </micro:ActionMessage>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                        <DataGrid.Columns>

                            <DataGridTemplateColumn Width="100" Header="Copy To Left">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="&lt;"  Visibility="{Binding CopyVisibility}" Width="60" Height="25" Padding="0" FontSize="20">
                                            <i:Interaction.Triggers>
                                                <i:EventTrigger EventName="Click">
                                                    <micro:ActionMessage MethodName="ParamCopyToLeft">
                                                        <micro:Parameter Value="{Binding }" />
                                                    </micro:ActionMessage>
                                                </i:EventTrigger>
                                            </i:Interaction.Triggers>
                                        </Button>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>


                            <DataGridTemplateColumn Header="Parameter Name" Width="180"  CanUserSort="True" IsReadOnly="True">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding ParamName}" Margin="5,0"  FontSize="14" Foreground="{DynamicResource FG_Black}" HorizontalAlignment="Left" VerticalAlignment="Center" ToolTip="{Binding RelativeSource={RelativeSource Self}, Path=Text}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                                <DataGridTemplateColumn.CellStyle>
                                    <Style TargetType="DataGridCell">
                                        <Setter Property="Background" Value="{Binding Path=Background}" />
                                    </Style>
                                </DataGridTemplateColumn.CellStyle>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="Parameter Value" Width="*" CanUserSort="True" IsReadOnly="True">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding ParamValue}" Margin="5,0"  FontSize="14" Foreground="{DynamicResource FG_Black}" HorizontalAlignment="Left" VerticalAlignment="Center" ToolTip="{Binding RelativeSource={RelativeSource Self}, Path=Text}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                                <DataGridTemplateColumn.CellStyle>
                                    <Style TargetType="DataGridCell">
                                        <Setter Property="Background" Value="{Binding Path=Background}" />
                                    </Style>
                                </DataGridTemplateColumn.CellStyle>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                </Grid>
            </Border>

        </Grid>-->



    </Grid>


</UserControl>
