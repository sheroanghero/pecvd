<ResourceDictionary
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d">

    <Geometry x:Key="UpArrow">M 0,4 L 3.5,0 L 7,4 Z</Geometry>

    <MenuScrollingVisibilityConverter x:Key="MenuScrollingVisibilityConverter"/>

    <Geometry x:Key="DownArrow">M 0,0 L 3.5,4 L 7,0 Z</Geometry>

    <Style x:Key="MenuScrollButton" BasedOn="{x:Null}" TargetType="{x:Type RepeatButton}">
        <Setter Property="ClickMode" Value="Hover"/>
        <Setter Property="MinWidth" Value="0"/>
        <Setter Property="MinHeight" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RepeatButton}">
                    <DockPanel SnapsToDevicePixels="true" Background="Transparent">
                        <Rectangle x:Name="R1" Fill="Transparent" Width="1" DockPanel.Dock="Right"/>
                        <Rectangle x:Name="B1" Fill="Transparent" Height="1" DockPanel.Dock="Bottom"/>
                        <Rectangle x:Name="L1" Fill="Transparent" Width="1" DockPanel.Dock="Left"/>
                        <Rectangle x:Name="T1" Fill="Transparent" Height="1" DockPanel.Dock="Top"/>
                        <ContentPresenter x:Name="ContentContainer" HorizontalAlignment="Center" Margin="2,2,2,2" VerticalAlignment="Center"/>
                    </DockPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsPressed" Value="true">
                            <Setter Property="Fill" TargetName="R1" Value="{DynamicResource {x:Static SystemColors.ControlLightLightBrushKey}}"/>
                            <Setter Property="Fill" TargetName="B1" Value="{DynamicResource {x:Static SystemColors.ControlLightLightBrushKey}}"/>
                            <Setter Property="Fill" TargetName="L1" Value="{DynamicResource {x:Static SystemColors.ControlDarkDarkBrushKey}}"/>
                            <Setter Property="Fill" TargetName="T1" Value="{DynamicResource {x:Static SystemColors.ControlDarkDarkBrushKey}}"/>
                            <Setter Property="Margin" TargetName="ContentContainer" Value="3,3,1,1"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="{ComponentResourceKey ResourceId=MenuScrollViewer, TypeInTargetAssembly={x:Type FrameworkElement}}" BasedOn="{x:Null}" TargetType="{x:Type ScrollViewer}">
        <Setter Property="HorizontalScrollBarVisibility" Value="Hidden"/>
        <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ScrollViewer}">
                    <Grid SnapsToDevicePixels="true">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Border Grid.Column="0" Grid.Row="1">
                            <ScrollContentPresenter Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <RepeatButton Style="{StaticResource MenuScrollButton}" Focusable="false" Command="{x:Static ScrollBar.LineUpCommand}" CommandTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}" Grid.Column="0" Grid.Row="0">
                            <RepeatButton.Visibility>
                                <MultiBinding FallbackValue="Visibility.Collapsed" Converter="{StaticResource MenuScrollingVisibilityConverter}" ConverterParameter="0">
                                    <Binding Path="ComputedVerticalScrollBarVisibility" RelativeSource="{RelativeSource TemplatedParent}"/>
                                    <Binding Path="VerticalOffset" RelativeSource="{RelativeSource TemplatedParent}"/>
                                    <Binding Path="ExtentHeight" RelativeSource="{RelativeSource TemplatedParent}"/>
                                    <Binding Path="ViewportHeight" RelativeSource="{RelativeSource TemplatedParent}"/>
                                </MultiBinding>
                            </RepeatButton.Visibility>
                            <Path Fill="{DynamicResource {x:Static SystemColors.MenuTextBrushKey}}" Data="{StaticResource UpArrow}"/>
                        </RepeatButton>
                        <RepeatButton Style="{StaticResource MenuScrollButton}" Focusable="false" Command="{x:Static ScrollBar.LineDownCommand}" CommandTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}" Grid.Column="0" Grid.Row="2">
                            <RepeatButton.Visibility>
                                <MultiBinding FallbackValue="Visibility.Collapsed" Converter="{StaticResource MenuScrollingVisibilityConverter}" ConverterParameter="100">
                                    <Binding Path="ComputedVerticalScrollBarVisibility" RelativeSource="{RelativeSource TemplatedParent}"/>
                                    <Binding Path="VerticalOffset" RelativeSource="{RelativeSource TemplatedParent}"/>
                                    <Binding Path="ExtentHeight" RelativeSource="{RelativeSource TemplatedParent}"/>
                                    <Binding Path="ViewportHeight" RelativeSource="{RelativeSource TemplatedParent}"/>
                                </MultiBinding>
                            </RepeatButton.Visibility>
                            <Path Fill="{DynamicResource {x:Static SystemColors.MenuTextBrushKey}}" Data="{StaticResource DownArrow}"/>
                        </RepeatButton>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <ControlTemplate x:Key="{ComponentResourceKey ResourceId=SubmenuContent, TypeInTargetAssembly={x:Type FrameworkElement}}" TargetType="{x:Type ContentControl}">
        <Border SnapsToDevicePixels="True" Background="{DynamicResource Color_BG_SubMenuBG}" MinWidth="{TemplateBinding ActualWidth}" BorderThickness="1" BorderBrush="{DynamicResource Color_BD_SubMenu}" Padding="5,0">
            <ContentPresenter Margin="0,5" HorizontalAlignment="Left"/>
        </Border>
    </ControlTemplate>

    <ControlTemplate x:Key="{ComponentResourceKey ResourceId=TopLevelHeaderTemplateKey, TypeInTargetAssembly={x:Type MenuItem}}" TargetType="{x:Type MenuItem}">
        <ControlTemplate.Resources>
            <Storyboard x:Key="OnMouseOver">
                <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.Opacity)" Storyboard.TargetName="Highlight">
                    <EasingDoubleKeyFrame KeyTime="0" Value="0"/>
                    <SplineDoubleKeyFrame KeyTime="0:0:0.2" Value="1" KeySpline="0,0,1,0"/>
                </DoubleAnimationUsingKeyFrames>
            </Storyboard>
            <Storyboard x:Key="OnMouseOut">
                <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.Opacity)" Storyboard.TargetName="Highlight">
                    <EasingDoubleKeyFrame KeyTime="0" Value="1"/>
                    <SplineDoubleKeyFrame KeyTime="0:0:0.2" Value="0" KeySpline="0,0,0,1"/>
                </DoubleAnimationUsingKeyFrames>
            </Storyboard>
        </ControlTemplate.Resources>
        <Grid SnapsToDevicePixels="true">
            <Border x:Name="BG" BorderBrush="{DynamicResource Color_BD_Menu}" BorderThickness="1" Cursor="Hand" Background="{DynamicResource Color_BG_Menu}">
                <Border.Effect>
                    <DropShadowEffect Color="#FFC6E9FF" ShadowDepth="0" BlurRadius="10"/>
                </Border.Effect>
                <Grid>
                    <Border x:Name="Highlight" Background="{DynamicResource Color_BG_Menu_Highlight}" Opacity="0">
                        <Border.Effect>
                            <BlurEffect/>
                        </Border.Effect>
                    </Border>
                </Grid>
            </Border>
            <ContentPresenter x:Name="contentPresenter" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" ContentSource="Header" RecognizesAccessKey="True" HorizontalAlignment="Center" VerticalAlignment="Center" Cursor="Hand">
            </ContentPresenter>
            <Popup x:Name="PART_Popup" AllowsTransparency="true" HorizontalOffset="1" IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}" Placement="Bottom" PopupAnimation="{DynamicResource {x:Static SystemParameters.MenuPopupAnimationKey}}" VerticalOffset="2" Focusable="false" MinWidth="150" Grid.Column="1">
                <ContentControl x:Name="SubMenuBorder" IsTabStop="false" Template="{DynamicResource {ComponentResourceKey ResourceId=SubmenuContent, TypeInTargetAssembly={x:Type FrameworkElement}}}">
                    <ScrollViewer Style="{DynamicResource {ComponentResourceKey ResourceId=MenuScrollViewer, TypeInTargetAssembly={x:Type FrameworkElement}}}" CanContentScroll="true">
                        <ItemsPresenter Margin="2" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" Grid.IsSharedSizeScope="true" KeyboardNavigation.DirectionalNavigation="Cycle" KeyboardNavigation.TabNavigation="Cycle" Cursor="Hand"/>
                    </ScrollViewer>
                </ContentControl>
            </Popup>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsSuspendingPopupAnimation" Value="true">
                <Setter Property="PopupAnimation" TargetName="PART_Popup" Value="None"/>
            </Trigger>
            <Trigger Property="Icon" Value="{x:Null}"/>
            <Trigger Property="IsChecked" Value="true"/>
            <Trigger Property="HasDropShadow" SourceName="PART_Popup" Value="true"/>
            <Trigger Property="IsHighlighted" Value="true">
                <Trigger.ExitActions>
                    <BeginStoryboard x:Name="OnMouseOut_BeginStoryboard1" Storyboard="{StaticResource OnMouseOut}"/>
                </Trigger.ExitActions>
                <Trigger.EnterActions>
                    <BeginStoryboard x:Name="OnMouseOver_BeginStoryboard" Storyboard="{StaticResource OnMouseOver}"/>
                </Trigger.EnterActions>
            </Trigger>
            <DataTrigger Binding="{Binding Selected}" Value="True">
                <Setter Property="Opacity" TargetName="Highlight" Value="1"/>
            </DataTrigger>
            <Trigger Property="IsKeyboardFocused" Value="true"/>
            <Trigger Property="IsSubmenuOpen" Value="true"/>
            <Trigger Property="IsEnabled" Value="false">
                <Setter Property="Foreground" Value="#FF9A9A9A"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <SolidColorBrush x:Key="Menu_Main_BD" Color="#FF043963"/>
    <SolidColorBrush x:Key="Menu_Main_BG" Color="#FF346294"/>
    <SolidColorBrush x:Key="Menu_Main_BG_Highlight" Color="#FF91B0CF"/>
    <SolidColorBrush x:Key="Menu_Main_BG_Outer" Color="#FFCACACA"/>
    <SolidColorBrush x:Key="Menu_Main_BG_Outer_Red" Color="#FFFF0000"/>
    <SolidColorBrush x:Key="Menu_Main_BD_Highlight" Color="#FF88A0CE"/>
    <SolidColorBrush x:Key="Menu_Main_BD_Shadow" Color="#FF111A2B"/>
    <SolidColorBrush x:Key="Menu_Main_BG_Inner" Color="#FF346294"/>
    <SolidColorBrush x:Key="Menu_Main_BG_Outer_Highlight" Color="#FF91B0CD"/>
    <SolidColorBrush x:Key="Menu_Main_BG_Inner_Highlight" Color="#FF1A2730"/>

    <ControlTemplate x:Key="SubMunu_ShowOnTop" TargetType="{x:Type MenuItem}">
        <Grid x:Name="MainBG" SnapsToDevicePixels="true">
            <Border BorderBrush="{DynamicResource Menu_Main_BD}" BorderThickness="1" Background="{DynamicResource Menu_Main_BG_Outer}" CornerRadius="5">
                <Grid>
                    <Rectangle x:Name="InnerBG" Height="10" Fill="{DynamicResource Menu_Main_BG_Inner}" VerticalAlignment="Center"/>
                    <Border x:Name="BG" CornerRadius="3" Margin="2" Background="{DynamicResource Menu_Main_BG}">
                        <ContentPresenter x:Name="contentPresenter" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" ContentSource="Header" RecognizesAccessKey="True" HorizontalAlignment="Center" VerticalAlignment="Center" Cursor="Hand" Margin="5,0">
                        </ContentPresenter>
                    </Border>
                </Grid>
            </Border>
            <Popup x:Name="PART_Popup" AllowsTransparency="true" HorizontalOffset="0" IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}" Placement="Top" PopupAnimation="{DynamicResource {x:Static SystemParameters.MenuPopupAnimationKey}}" VerticalOffset="-2" Focusable="false" MinWidth="150" Grid.Column="1">
                <ContentControl x:Name="SubMenuBorder" IsTabStop="false" Template="{DynamicResource {ComponentResourceKey ResourceId=SubmenuContent, TypeInTargetAssembly={x:Type FrameworkElement}}}">
                    <ScrollViewer Style="{DynamicResource {ComponentResourceKey ResourceId=MenuScrollViewer, TypeInTargetAssembly={x:Type FrameworkElement}}}" CanContentScroll="true">
                        <ItemsPresenter Margin="2" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" Grid.IsSharedSizeScope="true" KeyboardNavigation.DirectionalNavigation="Cycle" KeyboardNavigation.TabNavigation="Cycle" Cursor="Hand"/>
                    </ScrollViewer>
                </ContentControl>
            </Popup>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsSuspendingPopupAnimation" Value="true">
                <Setter Property="PopupAnimation" TargetName="PART_Popup" Value="None"/>
            </Trigger>
            <Trigger Property="Icon" Value="{x:Null}"/>
            <Trigger Property="IsChecked" Value="true"/>
            <Trigger Property="HasDropShadow" SourceName="PART_Popup" Value="true"/>
            <Trigger Property="IsHighlighted" Value="true">
                <Setter Property="Background" TargetName="BG" Value="{DynamicResource Menu_Main_BG_Highlight}"/>
                <Setter Property="Fill" TargetName="InnerBG" Value="{DynamicResource Menu_Main_BG_Highlight}"/>
            </Trigger>
            <DataTrigger Binding="{Binding Selected}" Value="True">
                <Setter Property="Foreground" Value="{DynamicResource FG_Black}"/>
                <Setter Property="Background" TargetName="BG" Value="{DynamicResource Menu_Main_BG_Highlight}"/>
                <Setter Property="Fill" TargetName="InnerBG" Value="{DynamicResource Menu_Main_BG_Inner_Highlight}"/>
                <Setter Property="Margin" TargetName="BG" Value="3"/>
                <Setter Property="Margin" TargetName="MainBG" Value="-3"/>
            </DataTrigger>
            <Trigger Property="IsKeyboardFocused" Value="true"/>
            <Trigger Property="IsSubmenuOpen" Value="true"/>
            <Trigger Property="IsEnabled" Value="false">
                <Setter Property="Foreground" Value="#FF9A9A9A"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <ControlTemplate x:Key="SubMunu_ShowOnTop_Red" TargetType="{x:Type MenuItem}">
        <Grid x:Name="MainBG" SnapsToDevicePixels="true">
            <Border BorderBrush="{DynamicResource Menu_Main_BD}" BorderThickness="1" Background="{DynamicResource Menu_Main_BG_Outer_Red}" CornerRadius="5">
                <Grid>
                    <Rectangle x:Name="InnerBG" Height="10" Fill="{DynamicResource Menu_Main_BG_Inner}" VerticalAlignment="Center"/>
                    <Border x:Name="BG" CornerRadius="3" Margin="2" Background="{DynamicResource Menu_Main_BG}">
                        <ContentPresenter x:Name="contentPresenter" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" ContentSource="Header" RecognizesAccessKey="True" HorizontalAlignment="Center" VerticalAlignment="Center" Cursor="Hand" Margin="5,0">
                        </ContentPresenter>
                    </Border>
                </Grid>
            </Border>
            <Popup x:Name="PART_Popup" AllowsTransparency="true" HorizontalOffset="0" IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}" Placement="Top" PopupAnimation="{DynamicResource {x:Static SystemParameters.MenuPopupAnimationKey}}" VerticalOffset="-2" Focusable="false" MinWidth="150" Grid.Column="1">
                <ContentControl x:Name="SubMenuBorder" IsTabStop="false" Template="{DynamicResource {ComponentResourceKey ResourceId=SubmenuContent, TypeInTargetAssembly={x:Type FrameworkElement}}}">
                    <ScrollViewer Style="{DynamicResource {ComponentResourceKey ResourceId=MenuScrollViewer, TypeInTargetAssembly={x:Type FrameworkElement}}}" CanContentScroll="true">
                        <ItemsPresenter Margin="2" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" Grid.IsSharedSizeScope="true" KeyboardNavigation.DirectionalNavigation="Cycle" KeyboardNavigation.TabNavigation="Cycle" Cursor="Hand"/>
                    </ScrollViewer>
                </ContentControl>
            </Popup>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsSuspendingPopupAnimation" Value="true">
                <Setter Property="PopupAnimation" TargetName="PART_Popup" Value="None"/>
            </Trigger>
            <Trigger Property="Icon" Value="{x:Null}"/>
            <Trigger Property="IsChecked" Value="true"/>
            <Trigger Property="HasDropShadow" SourceName="PART_Popup" Value="true"/>
            <Trigger Property="IsHighlighted" Value="true">
                <Setter Property="Background" TargetName="BG" Value="{DynamicResource Menu_Main_BG_Highlight}"/>
                <Setter Property="Fill" TargetName="InnerBG" Value="{DynamicResource Menu_Main_BG_Highlight}"/>
            </Trigger>
            <DataTrigger Binding="{Binding Selected}" Value="True">
                <Setter Property="Foreground" Value="{DynamicResource FG_Black}"/>
                <Setter Property="Background" TargetName="BG" Value="{DynamicResource Menu_Main_BG_Highlight}"/>
                <Setter Property="Fill" TargetName="InnerBG" Value="{DynamicResource Menu_Main_BG_Inner_Highlight}"/>
                <Setter Property="Margin" TargetName="BG" Value="3"/>
                <Setter Property="Margin" TargetName="MainBG" Value="-3"/>
            </DataTrigger>
            <Trigger Property="IsKeyboardFocused" Value="true"/>
            <Trigger Property="IsSubmenuOpen" Value="true"/>
            <Trigger Property="IsEnabled" Value="false">
                <Setter Property="Foreground" Value="#FF9A9A9A"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <ControlTemplate x:Key="SubMenu_ShowOnBottom" TargetType="{x:Type MenuItem}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition />
                <ColumnDefinition Width="10"/>
            </Grid.ColumnDefinitions>
            <Path x:Name="Left_Part" Data="M11.5,34.5 L10,34.5 0.5,0.5 11.5,0.5" Fill="{DynamicResource SubMenu_BG}" Margin="0,0,-3,0" Stretch="Fill" Stroke="{DynamicResource SubMenu_BD_Shadow}"/>
            <Path x:Name="Right_Part" Data="M11.5,34.5 L10,34.5 0.5,0.5 11.5,0.5" Fill="{DynamicResource SubMenu_BG}" Margin="-3,0,0,0" Stretch="Fill" Stroke="{DynamicResource SubMenu_BD_Shadow}" Grid.Column="2" RenderTransformOrigin="0.5,0.5">
                <Path.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform ScaleY="1" ScaleX="-1"/>
                        <SkewTransform AngleY="0" AngleX="0"/>
                        <RotateTransform Angle="0"/>
                        <TranslateTransform/>
                    </TransformGroup>
                </Path.RenderTransform>
            </Path>
            <Border Grid.Column="1" x:Name="BG" BorderBrush="{DynamicResource SubMenu_BD_Shadow}" BorderThickness="0,1" Background="{DynamicResource SubMenu_BG}" Cursor="Hand">
                <ContentPresenter x:Name="contentPresenter" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" Margin="6,0" Cursor="Hand" ContentSource="Header" RecognizesAccessKey="True" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <Rectangle x:Name="Top_Highlight" Height="1" Fill="{DynamicResource SubMenu_BD_Highlight}" Grid.ColumnSpan="3" Margin="1,1" VerticalAlignment="Top"/>
            <Rectangle x:Name="Bottom_Shadow" Height="1" Fill="{DynamicResource SubMenu_BD_Shadow}" Grid.Column="1" Margin="1,1" VerticalAlignment="Bottom"/>
            <Path x:Name="Left_Highlight" Data="M0,0 L1,0 11.5,35 10.5,35 z" Fill="{DynamicResource SubMenu_BD_Highlight}" Stretch="Fill" Margin="1,1,-2.5,1"/>
            <Path x:Name="Right_Shadow" Grid.Column="2" Data="M0,0 L1,0 -9.5,33 -10.5,33 z" Fill="{DynamicResource SubMenu_BD_Shadow}" Margin="-2.5,1,1,1" Stretch="Fill" />
        </Grid>
        <ControlTemplate.Triggers>
            <DataTrigger Binding="{Binding Selected}" Value="True">
                <Setter Property="Panel.ZIndex" Value="100"/>
                <Setter Property="Background" TargetName="BG" Value="{DynamicResource SubMenu_BG_Select}"/>
                <Setter Property="BorderBrush" TargetName="BG" Value="{DynamicResource SubMenu_BD_Shadow_Select}"/>
                <Setter Property="Fill" TargetName="Left_Part" Value="{DynamicResource SubMenu_BG_Select}"/>
                <Setter Property="Stroke" TargetName="Left_Part" Value="{DynamicResource SubMenu_BD_Shadow_Select}"/>
                <Setter Property="Fill" TargetName="Right_Part" Value="{DynamicResource SubMenu_BG_Select}"/>
                <Setter Property="Stroke" TargetName="Right_Part" Value="{DynamicResource SubMenu_BD_Shadow_Select}"/>
                <Setter Property="Fill" TargetName="Top_Highlight" Value="{DynamicResource SubMenu_BD_Shadow_Highlight}"/>
                <Setter Property="Fill" TargetName="Left_Highlight" Value="{DynamicResource SubMenu_BD_Shadow_Highlight}"/>
                <Setter Property="Fill" TargetName="Bottom_Shadow" Value="{DynamicResource SubMenu_BD_Shadow_Select}"/>
                <Setter Property="Fill" TargetName="Right_Shadow" Value="{DynamicResource SubMenu_BD_Shadow_Select}"/>
                <Setter Property="Foreground" Value="{DynamicResource SubMenu_FG_Select}"/>
                <Setter Property="FontSize" Value="16"/>
            </DataTrigger>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter Property="Background" TargetName="BG" Value="{DynamicResource SubMenu_BG_Select}"/>
                <Setter Property="Fill" TargetName="Left_Part" Value="{DynamicResource SubMenu_BG_Select}"/>
                <Setter Property="Fill" TargetName="Right_Part" Value="{DynamicResource SubMenu_BG_Select}"/>
                <Setter Property="Foreground" Value="{DynamicResource SubMenu_FG_Select}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="false">
                <Setter Property="Foreground" Value="#FF9A9A9A"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style x:Key="BottomSubMenu" TargetType="MenuItem">
        <Setter Property="Command" Value="{Binding DataContext.MenuItemClickCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"/>
        <Setter Property="CommandParameter" Value="{ Binding }"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource SubMenu_FG}"/>
        <Setter Property="FontFamily" Value="Arial"/>
        <Setter Property="MinWidth" Value="150"/>
        <Setter Property="Height" Value="32"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="Normal"/>
        <Setter Property="Margin" Value="-3,0"/>
        <Setter Property="Template" Value="{StaticResource SubMenu_ShowOnBottom}"/>
    </Style>

    <Style x:Key="MainMenu" TargetType="MenuItem">
        <Setter Property="Command" Value="{Binding DataContext.MainMenuItemClickCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"/>
        <Setter Property="CommandParameter" Value="{ Binding }"/>
        <Setter Property="HorizontalContentAlignment" Value="{Binding HorizontalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"/>
        <Setter Property="VerticalContentAlignment" Value="{Binding VerticalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource FG_White}"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="FontWeight" Value="Normal"/>
        <Setter Property="MinWidth" Value="130"/>
        <Setter Property="Margin" Value="0,0,30,0"/>
        <Setter Property="Height" Value="40"/>
        <Setter Property="Template" Value="{StaticResource SubMunu_ShowOnTop}"/>
        <Style.Triggers>
            <DataTrigger Binding="{Binding IsAlarm}" Value="True">
                <Setter Property="Template" Value="{StaticResource SubMunu_ShowOnTop_Red}"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>